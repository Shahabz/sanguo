----------------------------------------
-- 事件
----------------------------------------

-- 所属按钮点击时调用
function MapThumbModOnEvent( nType, nControlID, value, gameObject )
	if nType == UI_EVENT_CLICK then
		
	elseif nType == UI_EVENT_PRESS then
		if value == 0 then
			
		elseif value == 1 then
			
		end
	end
end

-- 载入时调用
function MapThumbModOnAwake( gameObject )
	
end

-- 界面初始化时调用
function MapThumbModOnStart( gameObject )
	
end

-- 界面显示时调用
function MapThumbModOnEnable( gameObject )
	
end

-- 界面隐藏时调用
function MapThumbModOnDisable( gameObject )
	
end

-- 界面删除时调用
function MapThumbModOnDestroy( gameObject )
	
end

-- 每帧调用
function MapThumbModOnLogic( gameObject )
	
end


----------------------------------------
-- 自定
----------------------------------------
